# probably will not do types, method signatures will be manual checks
# but they should still be bidirectional, like the benefits of overloadnig or whatever with static analysis are good
# also nominal types to tag values
# not sure if static analysis would be worth it

type
  Type* = object
